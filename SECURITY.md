# Security

Do not commit real API tokens, Android signing keys, passwords, private coin databases, or other credentials.

If a credential is accidentally committed publicly, revoke or rotate it immediately. Removing it in a later commit is not sufficient because it may remain in Git history.
